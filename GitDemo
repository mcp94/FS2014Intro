import UIKit

class Car: NSObject{
    var make = ""
    var model = ""
    var year = 1885
    var color = UIColor(red:0.0, green: 0.0, blue:0.0, alpha: 1.0)
    
    init(make inMake:String,model inModel:String, year inYear:Int, color inColor:String){
        make = inMake
        model = inModel
        year = inYear
        color = inColor
        super.init()
    }
    
}
